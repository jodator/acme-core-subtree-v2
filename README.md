# acme-core

Open core of the acme platform: the gateway API and the packages required to run it.

```sh
pnpm install
pnpm -r run test
node apps/api/src/boot.js
```

Contributions welcome. Commits must carry a `Signed-off-by:` line (DCO).
