# Contributing

Sign off every commit (`git commit -s`). Do not commit generated files: the lockfile is maintained by
maintainers, and `packages/api-types` is generated by `pnpm --filter @acme/api run codegen`.
